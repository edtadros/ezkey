# Redirect to ezkey.app

This minimal directory exists only to satisfy GitHub Pages, which is configured to build from `/docs`.

**The canonical site is [ezkey.app](https://ezkey.app/)**, served by Cloudflare Workers (`site/` directory).

This `docs/` directory contains only a redirect page. To disable GitHub Pages entirely, turn it off in repository Settings → Pages.
